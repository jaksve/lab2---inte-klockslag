with Ada.Strings.Fixed,Text_io;use Ada.Strings.Fixed;procedure A is package p renames Text_io;G:String(1..Integer'value(P.Get_Line)):=P.Get_line;begin P.New_Line;for I in G'range loop if count(G(1..I),"v")=count(g(1..I),"h")and count(g(1..I),"b")=count(g(1..I),"f")then p.Put("Tillbaka till utgångspunkten efter"&i'image&" steg.");return;end if;end loop;P.Put("Kom aldrig tillbaka!");end;
